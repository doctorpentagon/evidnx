import { GraduationCap } from "lucide-react";
import { ComingSoon } from "@/components/ComingSoon";

export function LearnPage() {
  return (
    <ComingSoon
      icon={GraduationCap}
      title="Learn"
      description="Contextual glossary, tutorials, and interactive stats widgets — built next."
    />
  );
}
